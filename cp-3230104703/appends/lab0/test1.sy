// Input: 5 3
// Output: 1 1 1 1

int g = 10;

void testVoid() {
    return;
}

int testArray(int arr[]) {
    return arr[0];
}

int main(){
    int a = read();
    int b = read();

    if (a == 5 && b <= 5 && a >= 3 || b == 3) {
        write(1);
    }

    if (a <= b) {
        write(0);
    } else {
        write(1);
    }

    if (a >= b) {
        write(1);
    }

    int arr[2] = {1, 2};
    write(testArray(arr));

    testVoid();

    return 0;
}